name: exotic
version: "0.1.26"
enabled: false
immutable: false
description: "Exotic utilities — daemon, proxy, scientific computing"
files:
  # ── Daemon ─────────────────────────────────────────────────
  - "utils/daemon/*"
  # ── Proxy ──────────────────────────────────────────────────
  - "utils/proxy/*"
  - "flows/exotic/*"
imports: []
dependencies:
    - "langchain-community"
    - "spacy"
    - "matplotlib"
    - "hnswlib"
    - "python-a2a"
requires:
  - "core"
commands: []
