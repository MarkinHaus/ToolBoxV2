name: web
version: "0.1.26"
enabled: true
immutable: false
description: "Web server — HTTP/WebSocket workers, Starlette, icli_web + Minu mods"
files:
  # ── Workers ────────────────────────────────────────────────
  - "utils/workers/*"
  # ── Web Assets ─────────────────────────────────────────────
  - "dist/*"
  # ── Web Mods ───────────────────────────────────────────────
  - "mods/icli_web/*"
  - "mods/Minu/*"
imports: []
dependencies:
    - "websockets"
    - "webauthn"
    - "waitress"
requires:
  - "core"
commands:
  - "workers"
  - "http_worker"
  - "ws_worker"
