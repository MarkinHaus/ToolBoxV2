beautifulsoup4>=4.12.0
browser-use>=0.1.40
chromadb>=0.4.0
# duckduckgo-search >=5.0.0 # Provided by browser-use potentially, or add if direct use
gtts>=2.3.0
# keyboard>=0.13.5 # Often problematic, consider if truly needed server-side
langchain>=0.1.0
langchain-community>=0.0.20
langchain-core>=0.1.20
litellm>=1.30.0 # Updated to a more recent version
networkx>=3.1
numpy>=1.26.0
openai>=1.3.0
pebble>=5.0.0
pydantic>=2.5.0
pydub>=0.25.0
# pygments>=2.17.0 # Text highlighting, often a transitive dependency
# pyperclip>=1.8.0 # Clipboard ops, usually for client-side tools
requests>=2.31.0
# starlette>=0.35.0 # Web framework, might be from A2A/MCP or other server parts
tiktoken>=0.5.0
# tokenizers>=0.15.0 # Transitive from transformers usually
torch>=2.1.0
tqdm>=4.66.0
transformers>=4.35.0
# utils>=1.0.1 # Generic name, ensure it's the correct one if used directly
websockets>=12.0
# whisper>=1.1.10 # OpenAI Whisper, if used

scikit-learn>=1.3.0
# fuzzywuzzy>=0.18.0 # Consider rapidfuzz for performance
# graphiti-core[anthropic,google-genai,groq]>=0.10.5 # Specific tool, ensure it's used
sentence-transformers>=2.2.0
# python-git>=3.1.0 # GitPython, if used directly
rapidfuzz>=3.0.0
astor>=0.8.0
# taichi>=1.7.0 # Specific computation library, ensure used
nest-asyncio>=1.5.0
schedule>=1.2.0
python-levenshtein>=0.20.0 # For fuzzy matching if rapidfuzz not preferred
langchain-google-genai>=0.0.5 # For Google GenAI models with Langchain
qdrant-client[fastembed]>=1.7.0
pyvis>=0.3.0
matplotlib>=3.8.0
redis>=5.0.0
seaborn>=0.13.0

# Dependencies from EnhancedAgent and Builder
google-adk>=0.3.0
python-a2a[all]>=0.5.1 # Ensure [all] includes server/client as needed
mcp>=1.6.0 # For MCP server/client if used directly beyond ADK's toolset
google-cloud-aiplatform>=1.38.0 # For Vertex AI tools/models in ADK
pyyaml>=6.0 # For config loading
restrictedpython>=7.0 # If restricted code execution is used
docker>=7.0.0 # If Docker code execution is used
uvicorn>=0.25.0 # For running FastAPI based servers (A2A/FastMCP)
httpx>=0.25.0 # HTTP client, often by other libs like python-a2a or litellm
fastapi>=0.108.0 # If building FastAPI apps directly

# OpenTelemetry (optional, add if used)
# opentelemetry-api>=1.22.0
# opentelemetry-sdk>=1.22.0
# opentelemetry-exporter-otlp>=1.22.0
