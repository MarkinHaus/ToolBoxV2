/**
 * HUD Module Exports
 * ==================
 */

import { HudWebSocket } from './HudWebSocket.js';
import { WidgetArea } from './WidgetArea.js';
import { HudApp } from './HudApp.js';

// Named exports
export { HudWebSocket, WidgetArea, HudApp };

// Default export
export default { HudWebSocket, WidgetArea, HudApp };
